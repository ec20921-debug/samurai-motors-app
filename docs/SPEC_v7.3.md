# SAMURAI MOTORS
## 予約・業務管理・顧客対応システム v7 正式仕様書

**バージョン：v7.3**
**最終更新：2026-04-19**
**適用対象：顧客対応系（予約／洗車管理／決済／顧客チャット）**

---

## 📋 目次

1. 本書の目的
2. システムの位置づけとv7化の狙い
3. システム全体構成
4. Botと利用者の関係
5. フロー別の業務設計（予約〜完了まで）
6. 予約機能仕様
7. 業務管理機能仕様
8. 決済管理仕様
9. 顧客チャット運用仕様
10. データ構造（スプレッドシート）
11. システム設定と環境構成
12. セットアップ関数とAPIエンドポイント
13. **経営ダッシュボード（v7.3 新規）**
14. 開発フェーズと移行計画
15. 運用ルール
16. バージョン履歴

---

## 1. 本書の目的

Samurai Motors の顧客対応系（予約・洗車管理・決済・顧客チャット）を **v6 肥大化（GAS 6,000行超）** から脱却し、**v7（GAS 3,000〜3,500行）** で再構築するための正式仕様を定義する。
v7.3 は v7.2 で凍結した基本仕様に、**運用開始後のフィードバックに基づく機能追加・品質改善** を織り込んだ版である。

---

## 2. システムの位置づけ／v7化の狙い

### 課題（v6）
- GAS 単一プロジェクト 6,000 行超
- 通知遅延・重複送信
- デバッグコードとセットアップコードが本番に混入

### v7 の狙い
- 顧客対応系と社内業務系（日報・勤怠・経費・在庫）を **完全分離**
- **12ファイル構成** で各ファイル 500 行以内
- Telegram 非同期キュー方式で `doPost` 応答を1秒以内に固定
- スプレッドシート連動設定（料金／営業時間／祝日）

### v7.3 で追加された狙い
- **数字による経営管理** を可能にする専用ダッシュボード
- **料金体系の柔軟性**（シート更新のみで全体反映）
- **稼働中の安定性向上**（スタレッドトピックの自動復旧、誤通知の抑制）

---

## 3. システム全体構成

```
👤 顧客 → 📘 Facebook集客 → 🤖 予約Bot（Telegramミニアプリ）
                                     ↓
                          ⚙️ GAS v7 ← 📊 Googleスプレッドシート
                                     ↓          📅 Googleカレンダー
                          🤖 業務Bot ← 📁 Googleドライブ
                                     ↓
                          💬 管理グループ（フォーラムトピック）
                                     ↓
                          📈 経営ダッシュボード（v7.3 新規）
```

### 3つのTelegram要素

| 呼称 | 役割 | 使用者 | メイン言語 |
|------|------|--------|-----------|
| **予約Bot** | 顧客接点 | カンボジア人顧客 | クメール語＋英語 |
| **業務Bot** | 現場ミニアプリ | 現場スタッフ | クメール語＋日本語 |
| **管理グループ** | 日本側管理者通知（フォーラムトピック付き） | 日本人管理者 | 日本語 |

---

## 4. Bot と利用者の関係

| 呼称（統一） | 旧呼称 | 廃止/存続 |
|---|---|---|
| 予約Bot | Bot C, booking bot | 存続 |
| 業務Bot | Bot B, field bot | 存続 |
| 管理グループ | Admin Group | 存続 |
| 管理Bot | admin bot（Webhook） | **廃止** |

---

## 5. フロー別の業務設計

| 工程 | 主体 | 対象 |
|---|---|---|
| 予約受付 | 予約Bot | 顧客 |
| 予約確認通知 | GAS | 顧客 / Admin / カレンダー / BOOKINGS |
| 作業開始 | 業務Bot（job-manager.html） | 顧客 / Admin / JOBS |
| Before 写真4枚 | 業務Bot | 顧客 / Admin（sendMediaGroup） / Drive |
| 作業完了 | 業務Bot | 顧客 / Admin / JOBS |
| After 写真4枚 | 業務Bot | 顧客 / Admin / Drive |
| QR送信 | GAS（自動連携） | 顧客 |
| 支払スクショ受付 | 予約Bot | Admin / Drive / BOOKINGS |
| 24h未払催促 | 時間トリガー | 顧客 / Admin |

---

## 6. 予約機能仕様

### 6.1 プラン体系（料金設定シート連動）

| プラン | 内容 | セダン以下 | SUV以上 |
|---|---|---|---|
| A: KIYOME | Waterless wash + Tire shine | $12 / 30分 | $15 / 45分 |
| B: KAGAMI | A + Front 3 windows water-repellent | $17 / 40分 | $23 / 65分 |
| C: TAKUMI | A + All windows water-repellent | $20 / 50分 | $25 / 65分 |
| D: SHOGUN | A + Oil film removal + All windows | $32 / 80分 | $38 / 100分 |

### 6.2 🆕 Delivery fee（出張料）の動的加算 — v7.3 新規

**設計原則：料金設定シート「出張料」行を唯一の真実とする（$2 をコードにハードコードしない）**

- `getDispatchFee()` が料金設定シート「出張料」行（セダン/SUV列）を参照
- `getBasePriceFor()`：基本料金のみ
- `getDispatchFeeFor()`：出張料のみ
- `getPriceFor()` = 基本料金 + 出張料 **（合計を返す）**

#### 表示フロー（v7.3）

| 箇所 | 表示 |
|---|---|
| ミニアプリ プランカード | `$12` の下に小さく `+ $2 Delivery fee / + $2 ថ្លៃដឹកជញ្ជូន` |
| ミニアプリ 確認画面 | `Plan price / Delivery fee / Total` の3行内訳 |
| 顧客 Telegram 通知 | 💰 Plan / 🚚 Delivery fee / 💵 Total の3行内訳 |
| 管理グループ通知 | `料金: $12 + 出張料 $2 = 合計 $14` |
| Googleカレンダー説明 | `料金: $12 + 出張料 $2 = 合計 $14` |
| 予約シート `料金(USD)` 列 | **合計金額を保存**（QR/請求/ダッシュボードが自動で正しくなる） |

#### 🔮 将来の料金改定
料金設定シートの「出張料」行の値を変えるだけ（60秒キャッシュ後に全体反映）。コード変更不要。

### 6.3 🆕 定休日・祝日（v7.3 新規）

| 区分 | 扱い |
|---|---|
| 日曜日 | **定休日**（`CLOSED_WEEKDAYS = [0]`） |
| カンボジア公式祝日 | **定休日**（`CAMBODIA_HOLIDAYS` 配列に2026年・2027年固定祝日登録） |
| booking.html 側 | `CLOSED_DAYS` / `CAMBODIA_HOLIDAYS` を同期。日付セルに `Closed` / `Holiday` 英語バッジ表示、opacity 0.4 |

### 6.4 🆕 空き枠診断情報（v7.3 新規）

`findAvailableSlots()` の返値に `debug` フィールド追加：
```
events=3 past=0 conflict=4 overflow=0 blockedBy=[飯泉渡航 04/20 00:00-23:59]
```
管理者がカレンダーの全日イベント等で空き枠が消えた原因を即判別できるようにした。

---

## 7. 業務管理機能仕様

（v7.2 から変更なし、既存仕様を継承）

- 業務Bot：`job-manager.html` 起動
- 作業開始／Before写真4枚／作業完了／After写真4枚 の4ステップ
- 各ステップで **顧客 / Admin / シート（JOBS）** の3方向配信

---

## 8. 決済管理仕様

### 8.1 QR 自動送信フロー
After 写真4枚送信後に：
1. QR_CODES から `active=TRUE` の QR 画像を取得
2. 顧客へ QR 画像 + 金額（合計）を送信
3. Admin に `QR送信 - $X USD` 通知

### 8.2 🆕 支払スクショ判定の厳格化（v7.3 新規）

v7.2 では「駐車場所の写真」が支払スクショ誤認定される不具合があった。v7.3 で `findLatestUnpaidBooking()` に以下の条件を追加：

```javascript
// 進行状態 は「作業完了」のみ対象
if (jStatus !== '作業完了') continue;
// QR送信から48時間以内のみ対象
var WINDOW_MS = 48 * 60 * 60 * 1000;
if (!ts || (now - ts) > WINDOW_MS) continue;
```

### 8.3 🆕 非支払写真への軽い受領返信（v7.3 新規）

駐車場所写真など「支払スクショではない写真」への返信：
```
📸 សូមអរគុណសម្រាប់រូបថត! / Thanks for the photo!
ក្រុមការងាររបស់យើងបានទទួលហើយ។
Our team has received it.
```

支払い確認の自動返信は誤解を招くため出さない。

### 8.4 24h 未払催促
（v7.2 仕様を継承）

---

## 9. 顧客チャット運用仕様

### 9.1 1顧客=1トピック原則
- `CUSTOMERS.thread_id` で管理
- 存在すればそのトピック、なければ `createForumTopic` で新規作成

### 9.2 🆕 スタレッドトピック自動復旧（v7.3 新規）

**背景**：管理グループのチャットログを削除するとトピックも消え、`thread_id` がスタレ化して `sendMessage` が `message thread not found` エラーで失敗する。v7.2 では予約通知が Admin に届かない障害が実際に発生した。

**対策**：`sendToCustomerTopicWithRecovery()` を新設。

```
1回目 → 既存の thread_id で送信
  ↓ 失敗
2回目 → description が "thread not found" or "TOPIC_*_INVALID" なら
         CUSTOMERS.thread_id をクリア → createForumTopic 再作成 → リトライ
  ↓ それでも失敗
最終フォールバック → General（thread 指定なし）へ `⚠️ トピック送信失敗` 付きで投稿
```

通知消失ゼロを保証。

### 9.3 全顧客トピックリセット関数
`resetAllCustomerTopicIds()` — 管理グループのチャットログを完全削除した際のリセット用。次回の顧客メッセージ／予約で自動的に新しいトピックが作成される。

---

## 10. データ構造（スプレッドシート）

（v7.2 仕様を継承）

主要シート：顧客 / 予約 / 車両 / 作業 / スタッフ / チャット履歴 / QR画像 / 料金設定

### 10.1 料金設定シート（= Plan_Prices）

| プラン名 | セダン以下 | SUV以上 | 所要(セダン) | 所要(SUV) | 説明 |
|---|---|---|---|---|---|
| 清 KIYOME (A) | 12 | 15 | 30 | 45 | ... |
| 鏡 KAGAMI (B) | 17 | 23 | 40 | 65 | ... |
| 匠 TAKUMI (C) | 20 | 25 | 50 | 65 | ... |
| 将軍 SHOGUN (D) | 32 | 38 | 80 | 100 | ... |
| **出張料** | **2** | **2** | — | — | **全プラン共通で加算（v7.3で動的参照）** |
| 【設定】営業開始 | 9 | — | — | — | — |
| 【設定】営業終了 | 18 | — | — | — | — |
| 【設定】バッファ(分) | 30 | — | — | — | — |

---

## 11. システム設定と環境構成

（v7.2 から変更なし）
- `CONFIG` 定数 → PropertiesService
- タイムゾーン `Asia/Phnom_Penh`
- GAS 12ファイル構成（Config / Router / QueueManager / TelegramAPI / SheetHelpers / BookingBot / BookingLogic / FieldBot / JobManager / CustomerChat / ForumTopicManager / PaymentManager）

### 🆕 v7.3 で追加されたファイル
| ファイル | 責務 | 行数 |
|---|---|---|
| **BookingDashboard.gs** | 経営ダッシュボード（顧客スプレッドシート側） | 約600行 |

ダッシュボードは **「本番で毎日動くコード」には含めない**（メニュー経由の手動更新＋JST 7:30自動更新）ため、12ファイル3,500行目標に影響しない。

---

## 12. セットアップ関数とAPIエンドポイント

（v7.2 仕様を継承）
- `setupV7()` / `setupV7Triggers()` / `setupV7Sheets()` / `setupV7Folders()` / `setupV7Webhooks()`
- doGet/doPost API：`booking_init` / `booking_slots` / `booking_create` / `field_jobs_today` / `field_job_detail` / `job_start` / `job_upload_before` / `job_end` / `job_upload_after` / `plan_prices` / `status`

### 🆕 v7.3 追加
- `setupBookingDashboardMenu()`：スプレッドシート onOpen トリガー設置
- `setupBookingDashboardDailyTrigger()`：JST 7:30 自動更新トリガー
- `ensureBookingDashboard()` / `refreshBookingDashboard()`：ダッシュボード生成・更新

---

## 13. 🆕 経営ダッシュボード（v7.3 新規）

### 13.1 目的
日本側管理者が「数字による経営管理」を毎朝5秒で完了できること。

### 13.2 配置
**顧客対応系スプレッドシート** に `経営ダッシュボード` タブを自動生成。
カスタムメニュー「📊 ダッシュボード」から手動更新可能。

### 13.3 構成（9セクション）

| セクション | 内容 |
|---|---|
| ① バナー | Samurai Motors 経営ダッシュボード（Dark×Gold テーマ） |
| ② 期間セレクター | 今月 / 先月 / 今年 / 昨年 / 過去12ヶ月 |
| ③ KPIカード（6枚） | 売上合計 / 予約件数 / 平均単価 / 新規顧客数 / リピート率 / 未回収金額 |
| ④ 月次推移グラフ | 売上 + 予約件数の複合チャート |
| ⑤ プラン別円グラフ | A/B/C/D の構成比 |
| ⑥ 決済状態別円グラフ | 清算済み / QR送信済み / 未清算 / 要確認 |
| ⑦ 新規 vs リピート円グラフ | 新規顧客 vs リピート顧客 |
| ⑧ 日別棒グラフ | 当月の日別売上 |
| ⑨ プラン別売上ランキング | プラン別の売上額・件数順 |
| ⑩ LTV Top10 | 顧客別 累計売上 上位10名 |
| ⑪ アラートエリア | 未入金48h超 / 前日比±20%異常 等 |

### 13.4 データ算出ロジック
- `_経営キャッシュ` 隠しシートに中間集計を保存
- `COUNTIFS` / `SUMIFS` / `QUERY` を駆使しスプレッドシート関数で完結（GAS の重いループを避ける）
- Chart API で円グラフ／棒グラフ／複合チャートを生成

### 13.5 自動更新
- JST 7:30 の時間トリガーで毎朝自動更新
- onOpen で手動更新メニュー表示

---

## 14. 開発フェーズと移行計画

| フェーズ | 内容 | ステータス |
|---|---|---|
| Phase 0 | 準備作業 | ✅ 完了 |
| Phase 1 | 基盤コード（Config/Router/Queue/TelegramAPI/SheetHelpers） | ✅ 完了 |
| Phase 2 | 顧客チャット + フォーラムトピック基盤 | ✅ 完了 |
| Phase 3 | 予約機能（BookingBot / BookingLogic / booking.html） | ✅ 完了 |
| Phase 4 | 業務管理機能（FieldBot / JobManager / job-manager.html） | ✅ 完了 |
| Phase 5 | 決済管理（QR送信・スクショ受付・24h催促） | ✅ 完了 |
| **Phase 6（v7.3）** | **経営ダッシュボード・Delivery fee 動的化・定休日/祝日・トピック自動復旧** | ✅ **完了** |

### v6 廃止
2026-04-18 に v6 は完全廃止済み。

---

## 15. 運用ルール

### 15.1 絶対にやってはいけないこと
1. トークン・機密情報のハードコード
2. CacheService でキュー管理
3. doPost 内での重い処理
4. 同じ顧客のトピック重複作成
5. status 列に `paid` を入れる
6. **🆕 料金・出張料のコードハードコード**（必ず料金設定シートから取得）
7. 無許可のファイル削除／本番データ変更
8. main への force push

### 15.2 コード変更後のフロー
1. `.gs` 編集
2. `.txt` 同期（手動コピペ用）
3. `git add` → `git commit` → `git push`
4. GAS エディタへ手動コピペ + デプロイ更新

### 15.3 ラベル統一ルール
- 「dispatch fee」ではなく **「Delivery fee / ថ្លៃដឹកជញ្ជូន」** で統一（v7.3 で確定）
- 「休業」ではなく **「Closed / Holiday」** で統一（v7.3 で確定）

---

## 16. バージョン履歴

| バージョン | 日付 | 変更内容 |
|---|---|---|
| v7.0 | 2026-04-15 | 初版。予約→洗車→QR→入金→催促の1周フロー／12ファイル構成／CacheService vs ScriptProperties 方針 |
| v7.1 | 2026-04-15 | 設定シート連動 / doGet API 整備 |
| v7.2 | 2026-04-15 | 全フェーズ完了版として凍結 |
| **v7.3** | **2026-04-19** | **経営ダッシュボード新設 / Delivery fee 動的加算 / 日曜定休日 + カンボジア祝日 / スタレッドトピック自動復旧 / 支払スクショ誤認定修正 / 非支払写真への軽い受領返信 / 空き枠診断情報 / Closed・Holiday 英語ラベル統一** |

---

## v7.3 変更点サマリー（一枚要約）

| 領域 | v7.2 | v7.3 |
|---|---|---|
| Delivery fee | コード内で暗黙的に固定 | **料金設定シート「出張料」行から動的加算・内訳表示** |
| 定休日 | なし（全日営業） | **日曜定休 + カンボジア公式祝日** |
| 祝日表示 | — | **Closed / Holiday 英語バッジ** |
| 経営ダッシュボード | なし | **Dark×Gold テーマ 9セクション（KPI・円グラフ・LTV）** |
| トピック耐障害性 | スタレッドで通知消失 | **自動検知 → 再作成 → リトライ → General フォールバック** |
| 支払スクショ判定 | QR送信済みなら全て候補 | **進行状態=作業完了 + 48h以内のみ** |
| 非支払写真 | 無反応 | **軽い受領メッセージで安心感** |
| 空き枠デバッグ | ログのみ | **フロントに診断情報を返却** |

---

**Samurai Motors / System Specification v7.3**
**© 2026 Samurai Motors. All rights reserved.**
